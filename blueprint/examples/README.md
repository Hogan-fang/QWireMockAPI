# Blueprint Examples

This directory contains JSON request and response samples derived from:

- `schema/order_server.yaml`
- `schema/callback_server.yaml`

Subdirectories:

- `order/`: order service request / response examples
- `callback/`: callback service request / response examples
